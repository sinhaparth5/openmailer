<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="016-create-api-keys-table" author="openmailer">
        <createTable tableName="api_keys">
            <column name="id" type="BIGSERIAL" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="user_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_api_keys_user" references="users(id)"
                             deleteCascade="true"/>
            </column>

            <column name="key_name" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column name="api_key" type="VARCHAR(64)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="key_prefix" type="VARCHAR(10)">
                <constraints nullable="false"/>
            </column>

            <!-- Permissions -->
            <column name="scopes" type="TEXT[]"/>

            <column name="is_active" type="BOOLEAN" defaultValueBoolean="true"/>

            <!-- Usage tracking -->
            <column name="last_used_at" type="TIMESTAMP"/>
            <column name="expires_at" type="TIMESTAMP"/>

            <!-- Timestamps -->
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Indexes -->
        <createIndex indexName="idx_api_keys_user_id" tableName="api_keys">
            <column name="user_id"/>
        </createIndex>

        <createIndex indexName="idx_api_keys_key" tableName="api_keys">
            <column name="api_key"/>
        </createIndex>

        <createIndex indexName="idx_api_keys_active" tableName="api_keys">
            <column name="is_active"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>

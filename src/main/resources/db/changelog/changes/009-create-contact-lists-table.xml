<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="009-create-contact-lists-table" author="openmailer">
        <createTable tableName="contact_lists">
            <column name="id" type="BIGSERIAL" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="user_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_contact_lists_user" references="users(id)"
                             deleteCascade="true"/>
            </column>

            <column name="name" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column name="description" type="TEXT"/>

            <!-- Statistics (cached) -->
            <column name="total_contacts" type="INT" defaultValueNumeric="0"/>
            <column name="active_contacts" type="INT" defaultValueNumeric="0"/>

            <!-- Settings -->
            <column name="double_opt_in_enabled" type="BOOLEAN" defaultValueBoolean="true"/>

            <!-- Timestamps -->
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Indexes -->
        <createIndex indexName="idx_contact_lists_user_id" tableName="contact_lists">
            <column name="user_id"/>
        </createIndex>

        <!-- Unique constraint for list name per user -->
        <addUniqueConstraint tableName="contact_lists"
                           columnNames="user_id, name"
                           constraintName="unique_list_name_per_user"/>
    </changeSet>

</databaseChangeLog>

# OpenMailer Environment Variables for Railway Deployment

# Database Configuration (Railway PostgreSQL - auto-populated)
DATABASE_URL=${{Postgres.DATABASE_URL}}
DATABASE_USERNAME=${{Postgres.PGUSER}}
DATABASE_PASSWORD=${{Postgres.PGPASSWORD}}

# Redis Configuration (Railway Redis - auto-populated)
REDIS_HOST=${{Redis.REDIS_HOST}}
REDIS_PORT=${{Redis.REDIS_PORT}}
REDIS_PASSWORD=${{Redis.REDIS_PASSWORD}}

# Application Security
# Generate with: openssl rand -base64 32 | head -c 32
ENCRYPTION_KEY=your-32-character-encryption-key-here

# Application Base URL
APP_BASE_URL=${{RAILWAY_PUBLIC_DOMAIN}}

# Mail Configuration (Optional - configure your SMTP provider)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# Alternative Mail Providers:
# Gmail: smtp.gmail.com:587
# SendGrid: smtp.sendgrid.net:587 (username: apikey)
# AWS SES: email-smtp.us-east-1.amazonaws.com:587
# Mailgun: smtp.mailgun.org:587
